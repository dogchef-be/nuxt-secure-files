{
  "name": "nuxt-secure-files",
  "version": "1.0.1-alpha",
  "description": "Embed images and download files securely via API requests",
  "repository": "github:dogchef-be/nuxt-secure-files",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "main": "dist/module.js",
  "types": "dist/module.d.ts",
  "scripts": {
    "clean": "npm prune && rm -rf dist",
    "transpile": "tsc",
    "build": "npm run clean && npm run transpile"
  },
  "dependencies": {
    "@nuxtjs/axios": ">= 5.13.4"
  },
  "devDependencies": {
    "@nuxt/types": "^2.15.6",
    "@nuxt/typescript-build": "^2.1.0",
    "nuxt": "^2.15.6",
    "typescript": "^4.2.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
